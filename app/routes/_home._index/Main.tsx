import { Box } from '@chakra-ui/react';

export const Main = () => {
    return (
        <Box as={'main'}>
            {/* Main content area */}
            <h1>Welcome to the main content area!</h1>
        </Box>
    );
};
